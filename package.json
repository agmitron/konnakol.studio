{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/client/*",
    "apps/server"
  ],
  "devDependencies": {
    "lerna": "^6.1.0"
  },
  "scripts": {
    "build": "lerna run build"
  }
}
